name: "My Document Collection"
slug: "my-docs"
description: "A searchable database of my documents"
documents_dir: "./documents"

ocr:
  backend: "pymupdf"
  workers: 4

images:
  min_size: 50
  min_bytes: 5120
  page_scan_ratio: 0.8

pipeline:
  embeddings: false
  knowledge_graph: false
  redaction_analysis: false

serve:
  port: 8001
  title: "Document Search"
